# MCP 集成（swe-agent）

本文说明 SWE-agent 的 MCP (Model Context Protocol) 支持现状。

---

## 当前状态：无 MCP 实现

经过对 SWE-agent 源代码的全面分析，**SWE-agent 目前没有实现 MCP (Model Context Protocol) 集成**。

### 1.1 工具执行架构

SWE-agent 采用传统的工具执行方式，而非 MCP 协议：

```text
┌─────────────────────────────────────────────────────────────────┐
│                   SWE-agent 工具架构                             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Agent Loop                            │   │
│  │                         │                                │   │
│  │                         ▼                                │   │
│  │  ┌─────────────────────────────────────────────────┐    │   │
│  │  │              Tool Registry                       │    │   │
│  │  │  ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │    │   │
│  │  │  │ bash     │ │ search   │ │ edit             │ │    │   │
│  │  │  │ tool     │ │ tool     │ │ tool             │ │    │   │
│  │  │  └────┬─────┘ └────┬─────┘ └────────┬─────────┘ │    │   │
│  │  │       └─────────────┴────────────────┘           │    │   │
│  │  │              直接执行（无 MCP）                   │    │   │
│  │  └─────────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 工具实现方式

SWE-agent 的工具直接通过 Python 函数实现，主要包括：

- **文件操作**: `open`, `scroll_up`, `scroll_down`, `create`, `edit`
- **搜索工具**: `search`, `search_file`, `find_file`
- **执行工具**: `bash`, `bash_background`, `kill_background_process`
- **Git 工具**: `git_status`, `git_diff`, `git_patch`

这些工具通过 `@tool` 装饰器注册，直接在 SWE-agent 进程内执行，而非通过 MCP 协议与外部服务器通信。

---

## 2. 与 MCP 集成的对比

| 特性 | SWE-agent (当前) | MCP 集成方式 |
|-----|-----------------|-------------|
| **工具注册** | Python `@tool` 装饰器 | MCP `tools/list` 协议 |
| **工具执行** | 本地函数调用 | MCP `tools/call` 协议 |
| **外部扩展** | 需修改源码添加工具 | 配置即可添加 MCP 服务器 |
| **生态兼容** | SWE-agent 专用 | 兼容所有 MCP 服务器 |

---

## 3. 如果未来添加 MCP 集成

如果 SWE-agent 未来要实现 MCP 集成，可能需要：

1. **添加 MCP 客户端依赖**: 引入 `mcp` Python SDK
2. **配置管理**: 支持 `swe-agent.yaml` 中配置 `mcp_servers`
3. **工具桥接**: 将 MCP 工具转换为 SWE-agent 的 `Tool` 对象
4. **执行转发**: 将工具调用转发到 MCP 服务器

**可能的架构**:

```text
┌─────────────────────────────────────────────────────────────────┐
│              SWE-agent (未来可能的 MCP 集成)                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                  Agent Loop                              │   │
│  │                       │                                  │   │
│  │                       ▼                                  │   │
│  │  ┌─────────────────────────────────────────────────┐    │   │
│  │  │            Tool Registry                         │    │   │
│  │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐         │    │   │
│  │  │  │ 内置工具  │ │ MCP工具1 │ │ MCP工具2 │         │    │   │
│  │  │  │ (bash等) │ │ (filesystem│ │ (github) │         │    │   │
│  │  │  └────┬─────┘ └────┬─────┘ └────┬─────┘         │    │   │
│  │  │       │            │            │               │    │   │
│  │  │       ▼            └────────────┘               │    │   │
│  │  │  直接执行              │                         │    │   │
│  │  │                   MCP Client                     │    │   │
│  │  │                       │                         │    │   │
│  │  └───────────────────────┼─────────────────────────┘    │   │
│  │                          │                              │   │
│  └──────────────────────────┼──────────────────────────────┘   │
└─────────────────────────────┼──────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │   MCP Servers   │
                    │  (filesystem,   │
                    │   github, etc)  │
                    └─────────────────┘
```

---

## 4. 总结

- SWE-agent **目前没有 MCP 实现**
- 工具通过传统的 Python 函数方式直接实现
- 如需使用 MCP 工具生态，可考虑其他支持 MCP 的 Code Agent（如 Codex、Gemini CLI、OpenCode）

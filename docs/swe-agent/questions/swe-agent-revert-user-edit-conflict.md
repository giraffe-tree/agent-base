# SWE-agent Revert User Edit Conflict

## TL;DR（结论先行）

**当前 SWE-agent 的 checkpoint 用于"每步持久化、断点续传"，未实现用户可触发的文件级 revert/rollback 能力。** 因此"revert 时发现用户已编辑与源文件冲突"的文件冲突场景，在现有文档范围内不适用；无对应冲突检测或协商逻辑。

---

## 1. 为什么需要这个机制？

### 1.1 问题场景

在支持 revert 的系统中，可能出现：
- 用户手动编辑文件后触发 revert
- revert 操作与用户的编辑产生冲突
- 需要冲突检测和协商机制

SWE-agent 的现状：
- Checkpoint 仅用于内部持久化
- 无用户触发的文件级 revert
- 因此不存在上述冲突场景

### 1.2 核心挑战

| 挑战 | 假设支持 revert 时的处理 | SWE-agent 现状 |
|-----|------------------------|---------------|
| 冲突检测 | 需要对比文件版本 | 不适用 |
| 协商策略 | 用户选择/自动合并 | 不适用 |
| 数据一致性 | 确保 checkpoint 与文件一致 | 仅内部使用 |
| 用户体验 | 清晰的冲突提示 | 无此功能 |

---

## 2. 整体架构

### 2.1 SWE-agent Checkpoint 定位

```text
┌─────────────────────────────────────────────────────────────┐
│ SWE-agent Session Management                                 │
│ sweagent/agent/agents.py                                     │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ Checkpoint（内部使用）                                       │
│ - 每 step 持久化 trajectory                                 │
│ - 支持断点续传                                              │
│ - ❌ 不支持用户触发的 revert                                 │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 对比：假设的 Revert 系统

```text
┌─────────────────────────────────────────────────────────────┐
│ Hypothetical Revert System                                   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   User Edit ──┐                                             │
│               ▼                                             │
│   ┌─────────────────┐      ┌─────────────────┐             │
│   │ Conflict        │─────▶│ Resolution      │             │
│   │ Detection       │      │ Strategy        │             │
│   └─────────────────┘      └─────────────────┘             │
│           ▲                                                │
│           │                                                │
│   Revert ─┘                                                │
│                                                            │
│   需要：文件版本对比、冲突标记、用户协商                    │
│                                                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 现状分析

### 3.1 Session/Checkpoint 定位

根据 `docs/swe-agent/02-swe-agent-session-management.md`：

> SWE-agent 的 Session 被描述为"trajectory 为中心的有状态执行单元"；每个 step 都会保存 checkpoint，用于持久化与断点续传。

### 3.2 未发现的功能

| 功能 | 状态 | 说明 |
|-----|------|------|
| Session.revert | ❌ 未实现 | 无文件回滚能力 |
| 文件级 revert | ❌ 未实现 | 无用户触发的文件恢复 |
| 冲突检测 | ❌ 未实现 | 无版本对比逻辑 |
| 冲突提示 | ❌ 未实现 | 无协商界面 |

### 3.3 证据索引

- **Session/checkpoint 定位**：详见 `docs/swe-agent/02-swe-agent-session-management.md`
- 文档中**未出现**与"用户触发 revert + 恢复文件"相关的流程
- 未发现"用户已编辑与源文件冲突"等设计或实现说明

---

## 4. 结论

### 4.1 当前情况

| 项目 | 当前情况 |
|------|---------|
| 文件级 revert 文档 | 未在现有文档中体现 |
| 用户编辑 vs 源文件冲突 | 不适用（无用户触发的文件回滚流程） |
| 冲突检测/提示 | 无相关描述 |

### 4.2 一句话总结

当前文档下 SWE-agent 未体现用户触发的文件级 revert，因此不存在"revert 时发现用户已编辑与源文件冲突"的现状处理说明。

---

## 5. 延伸阅读

- 相关文档：`docs/swe-agent/02-swe-agent-session-management.md`（Session 管理详细分析）
- 对比分析：`docs/kimi-cli/questions/kimi-cli-checkpoint-implementation.md`（Kimi CLI 的 Checkpoint 回滚机制）

---

*⚠️ Inferred: 基于现有文档分析，非源码直接验证*
*基于版本：SWE-agent (baseline 2026-02-08) | 最后更新：2026-02-24*

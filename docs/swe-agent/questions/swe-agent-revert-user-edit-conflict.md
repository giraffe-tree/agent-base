# SWE-agent：使用 revert 回滚时，用户已编辑与源文件冲突怎么处理？

本文基于现有文档说明 **SWE-agent 当前**是否具备“revert 回滚时发现用户已编辑、与源文件冲突”的流程与处理。只讲现状，不讨论未来可如何实现。

---

## 1) 结论

**当前文档中 SWE-agent 的 checkpoint 用于“每步持久化、断点续传”，未描述用户可触发的、文件级的 revert/rollback 能力。**  
因此“revert 时发现用户已经编辑过、与源文件冲突怎么处理”的**文件冲突**场景，在现有文档范围内不适用；无对应冲突检测或协商逻辑可述。

---

## 2) 依据

- **Session/checkpoint 定位**：SWE-agent 的 Session 被描述为“trajectory 为中心的有状态执行单元”；每个 step 都会保存 checkpoint，用于持久化与断点续传。  
  详见 `docs/swe-agent/02-swe-agent-session-management.md`。
- 文档中**未出现**“Session.revert”“文件回滚”“revert 到某 checkpoint 并恢复工作区文件”等与“用户触发 revert + 恢复文件”相关的流程。
- 未发现“用户已编辑与源文件冲突”“冲突检测”“冲突提示”等与 revert 冲突处理相关的设计或实现说明。

故基于现有文档，**视为无用户触发的文件级 revert，进而无“用户已编辑与源文件冲突”的当前处理**。

---

## 3) 小结

| 项目                 | 当前情况                                         |
|----------------------|--------------------------------------------------|
| 文件级 revert 文档   | 未在现有文档中体现（checkpoint 用于步进持久化）   |
| 用户编辑 vs 源文件冲突 | 不适用（无用户触发的文件回滚流程可依）            |
| 冲突检测/提示        | 无相关描述                                       |

---

**一句话**：当前文档下 SWE-agent 未体现用户触发的文件级 revert，因此不存在“revert 时发现用户已编辑与源文件冲突”的现状处理说明。
